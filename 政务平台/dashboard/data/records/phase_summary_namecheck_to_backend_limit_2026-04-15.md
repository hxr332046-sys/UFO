# 阶段性总结：name-check 前端门控清零与后端限流阻断（2026-04-15）

## 阶段目标

- 继续从 `core.html#/flow/base/name-check-info` 推进，突破“行业/组织形式/须知勾选”门控。
- 在不改业务逻辑前提下，通过 CDP 与组件级逆向方式把流程推进到下一环节。
- 若仍阻断，明确阻断层级（前端/后端）并固化证据。

## 本阶段完成情况

- **前端门控已基本清空**：
  - `请选择行业/经营特点` 已清空。
  - `请选择组织形式` 已清空。
  - `formInfo.isCheckBox` 已为 `Y`，同意提示勾选状态已同步。
- **仍未进入下一页**：页面持续停留在 `#/flow/base/name-check-info`。
- **新阻断已明确为后端层**：页面稳定出现服务端异常提示：
  - `您好，由于系统服务出现异常，请勿多次重复提交，耐心等待片刻再重新尝试...`

## 关键技术发现

- `organization-select` 并非仅看 `formInfo.organize`，还依赖内部字段联动（例如 `zhongjainzhi` / 输入框态 / 组件事件链）。
- 通过“代码+文本”双态同步后，组织形式错误可被消除；该问题属于组件态同步问题，非纯字段赋值问题。
- 在前端错误清空后，点击“保存并下一步”仍不跳转，且出现统一服务端异常文案，说明当前主要瓶颈已经从前端校验切换为后端限流/保护策略。

## 证据文件（本阶段）

- `fix_org_by_label_and_input.json`  
  组织形式通过代码+名称联动后，前端错误清空的证据。
- `set_namecheck_checkbox_and_next.json`  
  `isCheckBox=Y`、`declarationMode=Y` 已生效且提交后的状态证据。
- `accept_notice_and_next_namecheck.json`  
  执行“勾选+确定+保存”后仍停留当前页的证据。
- `continue_core_to_yunbangban.json`  
  当前自动续跑结果，显示仍停在 `name-check-info`，最终错误为服务端异常提示。
- `hook_namecheck_org_chain.json`  
  组织形式链路钩子追踪，确认 `getFormPromise` 可返回 `true`，但流程仍可被后续门控拦截。
- `inspect_organization_select_runtime.json`  
  组织形式组件运行时结构、`groupList` 样本与内部字段证据。

## 当前结论

- 当前不再是“字段不会填”问题，前端关键门控已基本打通。
- 主阻断已迁移为后端异常/限流保护，导致“保存并下一步”无法推进到后续节点。
- 在当前会话与频率下，直接重试会持续触发同一后端错误文案。

## 下一步建议

- 采用“冷却窗口 + 单次提交”策略（建议 5-10 分钟冷却后仅提交 1 次）。
- 若仍失败，抓取该次提交对应的后端响应体与错误码（非页面文案），进一步定位是否为风控、频控或会话污染。
- 必要时重新拉起新业务实例（新会话新链路）验证是否可绕开当前实例级限流状态。

